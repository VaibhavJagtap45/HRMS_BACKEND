# ── Database ─────────────────────────────────────────────────────────────────
MONGO_URI=mongodb+srv://HRMS:HRMS123@cluster0.9eaas49.mongodb.net/?appName=Cluster0

# ── Authentication ───────────────────────────────────────────────────────────
JWT_SECRET=your_super_secret_key_change_this_in_production
JWT_EXPIRES_IN=8h
BCRYPT_SALT_ROUNDS=10

# ── Server ───────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development

# ── CORS ─────────────────────────────────────────────────────────────────────
# Set to your deployed Next.js domain in production
CLIENT_URL=http://localhost:3000


# ── Email / SMTP (for Forgot Password) ────────────────────────────────────────
# Fill these in to enable real email sending.
# For Gmail: use an App Password (Google Account → Security → App Passwords).
# Leave blank to get the reset URL in the API response (dev/testing mode).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=albostechnologies016@gmail.com
SMTP_PASS=nyab sgwt qwes lpte
EMAIL_FROM=HRMS Albos
